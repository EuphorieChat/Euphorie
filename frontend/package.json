{
  "name": "euphorie-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "start": "python3 -m http.server 5173",
    "build": "mkdir -p build && cp *.html build/ && cp -r static build/ 2>/dev/null || true && cp -r src build/ 2>/dev/null || true && cp *.png *.ico *.webmanifest build/ 2>/dev/null || true"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/camera": "^7.0.2",
    "@capacitor/cli": "^7.4.3",
    "@capacitor/core": "^7.4.3",
    "@capacitor/ios": "^7.4.3"
  },
  "type": "module"
}
