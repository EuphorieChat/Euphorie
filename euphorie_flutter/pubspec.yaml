name: euphorie_flutter
description: Euphorie AI - Complete AR Vision System
publish_to: 'none'
version: 2.0.4+14

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  http: ^1.2.0
  dio: ^5.7.0
  speech_to_text: ^7.0.0 
  flutter_tts: ^4.1.0
  shared_preferences: ^2.3.0
  flutter_animate: ^4.5.0
  shimmer: ^3.0.0
  webview_flutter: ^4.10.0
  permission_handler: ^12.0.1
  intl: ^0.20.0
  url_launcher: ^6.3.1
  vector_math: ^2.1.4
  
  # 🖼️ Image Processing & Compression
  
  # 💎 RevenueCat Subscriptions
  purchases_flutter: ^9.9.6
  purchases_ui_flutter: ^9.9.7
  
  # 🔑 Native OAuth Authentication (NO FIREBASE/SUPABASE!)
  google_sign_in: 6.2.1
  sign_in_with_apple: ^6.1.3
  flutter_secure_storage: ^9.2.2      # Secure token storage
  jwt_decoder: ^2.0.1                 # Decode OAuth tokens
  crypto: ^3.0.5                      # Encryption utilities
  flutter_web_auth_2: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/logo.png